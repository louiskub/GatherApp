### โปรแกรมที่ต้องการ
```
- dotnet8
- docker
```

### วิธีการติดตั้ง
```
docker compose up -d
dotnet tool install --global dotnet-ef
dotnet restore 
dotnet ef database update
dotnet run
```

### 
Database Manager = localhost:8888
