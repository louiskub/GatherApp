@{
    ViewData["Title"] = "Gather-Home";
}

@* @{
    var isAuthorized = ViewBag.IsAuthorized as bool? ?? true;
} *@



    <link rel="stylesheet" href="~/css/components/dropdown.css">
    <link rel="stylesheet" href="~/css/components/post.css">
    <link rel="stylesheet" href="~/css/components/post_in_date.css">

    @* @await Html.PartialAsync("_NavUnAuth") *@
    <link rel="stylesheet" href="~/css/home/home.css">

    <div class="main">
        <button class="dropdown-toggle" href="#">
            <i class="fas fa-sliders dropdown-icon"></i> Filter
        </button>

        <div class="sidenav"></div>
        <div class="home-content">
            <div class="write-button">
                <img src="https://cdn-icons-png.flaticon.com/512/84/84380.png" alt="Write">
            </div>
        </div>
    </div>
    @* @if (isAuthorized)
{
    <p>แสดงเมนูสำหรับผู้ใช้ที่ล็อกอินแล้ว</p>
}
else
{
    <p>แสดงเมนูสำหรับผู้ใช้ที่ยังไม่ล็อกอิน</p>
} *@
    <script type="module" src="~/js/home/home.js"></script>